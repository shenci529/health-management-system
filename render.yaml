services:
  - type: web
    name: health-management-system
    env: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm run server:prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
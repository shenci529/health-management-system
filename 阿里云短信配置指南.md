# 阿里云短信服务配置指南

## 📋 配置步骤概览

```
1. 注册阿里云账号
2. 开通短信服务
3. 申请短信签名
4. 申请短信模板
5. 获取 AccessKey
6. 填写配置文件
7. 测试发送
```

---

## 详细配置步骤

### 第一步：注册阿里云账号

访问：https://www.aliyun.com

- 使用手机号或邮箱注册
- 个人开发者可以直接注册，无需企业资质

---

### 第二步：开通短信服务

1. 登录阿里云控制台
2. 顶部搜索框搜索「短信服务」
3. 点击进入产品页面
4. 点击「立即开通」

> 首次开通有免费额度：100条短信

---

### 第三步：申请短信签名

短信签名是短信发送方的标识，比如您的公司/学校名称。

1. 进入「短信服务」→「国内消息」→「短信签名管理」
2. 点击「添加签名」
3. 填写信息：
   - **签名名称**：`健康管理系统`（或其他您喜欢的名字）
   - **签名来源**：选择「已上线网站地址」或「App应用名称」
   - **申请说明**：简要说明用途，如「用于学生健康管理系统用户登录验证」

4. 等待审核（通常 1-2 小时，节假日可能延长）

---

### 第四步：申请短信模板

短信模板是短信的内容格式。

1. 进入「短信服务」→「国内消息」→「短信模板管理」
2. 点击「添加模板」
3. 填写信息：
   - **模板名称**：`验证码模板`
   - **模板类型**：选择「验证码」
   - **模板内容**：`您的验证码是${code}，5分钟有效，请勿泄露给他人。`
   - **申请说明**：简要说明用途

4. 等待审核（通常 1-2 小时）

> ⚠️ 审核通过后，你会获得一个 **模板CODE**，类似 `SMS_xxxxxx`

---

### 第五步：获取 AccessKey

AccessKey 是调用阿里云 API 的凭证。

1. 登录阿里云控制台
2. 点击右上角头像 → 「AccessKey 管理」
3. 点击「创建 AccessKey」
4. 保存生成的 **AccessKey ID** 和 **AccessKey Secret**

> ⚠️ AccessKey Secret 只显示一次，请立即保存！

---

### 第六步：填写配置文件

编辑 `server/smsService.js`，填写您的配置：

```javascript
const config = {
  // 阿里云 AccessKey ID（从阿里云控制台获取）
  accessKeyId: '您的AccessKeyId',  // ← 替换这里

  // 阿里云 AccessKey Secret（从阿里云控制台获取）
  accessKeySecret: '您的AccessKeySecret',  // ← 替换这里

  // 短信签名名称（必须是审核通过的签名）
  signName: '健康管理系统',  // ← 替换这里（用您的签名名）

  // 短信模板CODE（审核通过后获得的模板代码）
  templateCode: 'SMS_xxxxxx'  // ← 替换这里
};
```

---

## 📊 费用说明

| 项目 | 免费额度 | 超出后价格 |
|-----|---------|----------|
| 首次开通 | 100 条 | 0.045 元/条 |
| 个人用户 | - | 约 4.5 分钱/条 |
| 企业用户 | 1000 条/月 | 约 3.5 分钱/条 |

**实际费用**：每条验证码短信约 **4-5 分钱**，1000条约 45 元

---

## 🔧 常见问题

### Q1: 签名审核不通过怎么办？

常见原因：
- 签名与网站/APP名称不符
- 使用了敏感词汇（如「银行」「官方」等）

解决方法：
- 使用真实的学校/公司名称
- 或使用个人名称

### Q2: 模板审核不通过怎么办？

常见原因：
- 变量格式错误
- 包含违规内容

解决方法：
- 变量格式必须是 `${code}` 这样的格式
- 不要包含「中奖」「贷款」等敏感词

### Q3: 短信发送失败怎么办？

检查清单：
1. ✅ AccessKey 配置是否正确
2. ✅ 签名是否已审核通过
3. ✅ 模板是否已审核通过
4. ✅ 手机号格式是否正确（86开头或纯数字11位）
5. ✅ 账户余额是否充足

---

## 🧪 本地测试

配置完成后，在项目目录运行：

```bash
cd server
npm install
node server.js
```

然后访问接口测试：
```bash
curl -X POST http://localhost:3002/api/parent/send-code \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800138000"}'
```

成功后会收到真实短信！

---

## 🚀 生产环境部署

推送到 GitHub 后，需要在 Render 控制台设置环境变量：

1. 登录 Render Dashboard
2. 选择您的后端服务
3. 进入「Environment」选项卡
4. 添加以下环境变量：
   - `ALIYUN_ACCESS_KEY_ID` = 您的AccessKeyId
   - `ALIYUN_ACCESS_KEY_SECRET` = 您的AccessKeySecret
   - `ALIYUN_SMS_SIGN_NAME` = 您的短信签名
   - `ALIYUN_SMS_TEMPLATE_CODE` = 您的模板CODE

5. 点击「Save Changes」，服务会自动重启

---

## 📞 阿里云帮助文档

- 短信服务控制台：https://dysms.console.aliyun.com
- 短信签名申请指南：https://help.aliyun.com/document_detail/108076.html
- 短信模板申请指南：https://help.aliyun.com/document_detail/108077.html
- SDK文档：https://help.aliyun.com/document_detail/112819.html

# 🌐 免费公网访问方案（无需注册）

## 方案对比

| 方案 | 需要注册 | 难度 | 稳定性 | 推荐度 |
|------|---------|------|--------|--------|
| **局域网访问** | ❌ 不需要 | ⭐ 简单 | ⭐⭐⭐⭐⭐ | 🥇 |
| **JSONBin云数据库** | ⚠️ 需要API Key（免费） | ⭐⭐ 中等 | ⭐⭐⭐⭐ | 🥈 |
| **部署到云平台** | ⚠️ GitHub账号 | ⭐⭐⭐ 中等 | ⭐⭐⭐⭐⭐ | 🥉 |

---

## 🥇 方案1：局域网访问（最简单）

### 适用场景
如果你的用户和服务器在**同一个网络**下（如同一个WiFi、同一栋楼的网络），可以直接使用内网IP访问。

### 步骤
1. 查找服务器IP地址
   ```bash
   # Windows
   ipconfig
   
   # 找到 IPv4 地址，例如：192.168.3.25
   ```

2. 访问地址
   - 前端：`http://192.168.3.25:3000`
   - 后端：`http://192.168.3.25:3002`

### 优点
- ✅ 完全免费
- ✅ 不需要任何配置
- ✅ 速度快（局域网）
- ✅ 稳定

### 缺点
- ❌ 不同网络下无法访问

---

## 🥈 方案2：JSONBin.io云数据库（推荐）

### 特点
- ✅ 不需要搭建服务器
- ✅ 免费使用
- ✅ API Key即用
- ✅ 支持JSON数据存储
- ✅ 可以存储异常记录等数据

### 使用步骤

#### 1. 创建JSONBin集合
1. 访问 https://jsonbin.io
2. 用邮箱登录（可以Google登录）
3. 点击 "Create Collection"
4. 创建集合，命名为 `abnormal_records`

#### 2. 获取API Key
1. 在 Dashboard 找到 "API Keys"
2. 复制你的 API Key（格式如：`$2a$10$xxxxxxxx`）

#### 3. 修改前端代码
将 `AbnormalHandle.vue` 中的API调用改为JSONBin：

```javascript
// JSONBin配置
const JSONBIN_API_KEY = '你的API Key';
const JSONBIN_COLLECTION_ID = '你的Collection ID';

// 获取异常记录
async loadData() {
  this.loading = true;
  try {
    const response = await fetch(
      `https://api.jsonbin.io/v3/c/${JSONBIN_COLLECTION_ID}/records`,
      {
        headers: {
          'X-API-Key': JSONBIN_API_KEY
        }
      }
    );
    const data = await response.json();
    this.tableData = data.record.records || [];
  } catch (error) {
    console.error('加载数据失败:', error);
  } finally {
    this.loading = false;
  }
}
```

#### 4. 优点
- ✅ 无需自建服务器
- ✅ 全球可访问
- ✅ 免费额度够用
- ✅ HTTPS安全

#### 5. 缺点
- ❌ 需要一个邮箱注册
- ❌ 免费版有请求限制

---

## 🥉 方案3：部署到Railway云平台

### 特点
- ✅ 完全免费（每月$5额度）
- ✅ 自动部署
- ✅ 自动HTTPS
- ✅ 全球CDN加速
- ✅ 固定URL

### 步骤

#### 1. 上传到GitHub
1. 在GitHub创建新仓库
2. 上传整个项目代码

#### 2. 部署后端到Railway
1. 访问 https://railway.app
2. GitHub登录
3. 点击 "New Project" → "Deploy from GitHub"
4. 选择你的仓库
5. Railway自动检测Node.js项目
6. 添加环境变量
7. 点击 Deploy

#### 3. 获取访问地址
Railway会提供类似：`https://health-api.up.railway.app` 的地址

#### 4. 修改前端配置
在代码中使用这个固定地址。

---

## ⚡ 最快方案：使用本地IP地址

### 如果你只需要：
- ✅ 同一WiFi下的其他电脑访问
- ✅ 手机访问（同一网络）
- ✅ 局域网内的平板访问

### 操作：
1. 确保服务器IP是 `192.168.3.25`（你的当前IP）
2. 其他设备访问：
   - 电脑浏览器：`http://192.168.3.25:3000`
   - 手机浏览器：`http://192.168.3.25:3000`

3. **确保防火墙允许**：
   - 打开 Windows 防火墙高级设置
   - 添加入站规则，允许 3000 和 3002 端口

---

## 🎯 我的推荐

### 如果你只是测试：
直接使用内网IP：`http://192.168.3.25:3000`

### 如果要给外部人员看：
1. 用JSONBin替代本地数据库
2. 前端使用JSONBin的公开URL

### 如果要正式使用：
1. 注册Railway（免费）
2. 一键部署，自动获得公网地址

---

## 📞 你想要哪个方案？

告诉我你的需求：
1. **只用局域网** → 我帮你配置防火墙
2. **用云数据库** → 我帮你改成JSONBin
3. **部署到云** → 我帮你部署到Railway

你想选哪个？

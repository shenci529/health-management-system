# 🚀 健康管理系统 - 云端部署指南

## 📋 部署方案选择

| 方案 | 难度 | 费用 | 稳定性 | 推荐 |
|------|------|------|--------|------|
| **Railway** | ⭐ 简单 | 免费额度 | ⭐⭐⭐⭐⭐ | 🥇 首选 |
| **Render** | ⭐ 简单 | 免费额度 | ⭐⭐⭐⭐⭐ | 🥈 备选 |
| **Vercel (前端)** | ⭐⭐ 中等 | 免费 | ⭐⭐⭐⭐ | 🥉 前端 |

---

## 🥇 方案1：部署到 Railway（推荐！）

### 优点
- ✅ 完全免费（每月 $5 额度，够用）
- ✅ 一键部署，无需配置服务器
- ✅ 自动 HTTPS
- ✅ 全球 CDN 加速
- ✅ 数据库自动配置
- ✅ 自动部署（GitHub 推送自动更新）

---

## 📝 详细步骤

### 第一步：准备项目文件

1. 确保项目代码完整
2. 打开 `server/package.json`，确认启动命令为：
   ```json
   "scripts": {
     "start": "node server.js",
     "init-db": "node database/init.js"
   }
   ```

### 第二步：上传到 GitHub

1. 访问 https://github.com/
2. 注册/登录账号
3. 点击右上角 "+" → New Repository
4. 填写仓库名称：`health-management-system`
5. 选择 Public 或 Private
6. 点击 Create repository
7. 按页面提示，将本地代码推送到 GitHub

### 第三步：部署到 Railway

1. 访问 https://railway.app/
2. 使用 GitHub 账号登录（点击 "Continue with GitHub"）
3. 点击 "New Project" → "Deploy from GitHub repo"
4. 选择你的仓库 `health-management-system`
5. 点击 "Configure" 进行配置

### 第四步：配置项目

#### 1. 环境变量配置
在 Railway 项目设置中，添加以下环境变量：
```
NODE_ENV=production
PORT=3002
```

#### 2. 数据库配置
Railway 会自动提供 SQLite 支持（项目内已配置好）

### 第五步：部署完成！

等待 2-5 分钟，部署成功后，Railway 会提供一个公网地址：
```
https://xxx.railway.app
```

---

## 🌐 访问地址

部署成功后：
- 前端页面：`https://xxx.railway.app`（或你的自定义域名）
- 后端 API：`https://xxx.railway.app/api`

---

## 📱 修改前端连接地址

部署后，需要修改前端代码中的 API 地址：

打开 `src/views/AbnormalHandle.vue`，将所有：
```javascript
http://192.168.3.25:3002
```
替换为：
```javascript
/api
```

这样前端和后端会在同一域名下，使用相对路径，更安全！

---

## 🥈 方案2：部署到 Render（备选）

### 步骤
1. 访问 https://render.com/
2. 使用 GitHub 登录
3. 点击 "New" → "Web Service"
4. 选择你的仓库
5. 配置启动命令：`cd server && npm start`
6. 配置端口：3002
7. 点击部署

---

## 💻 本地永久运行（可选）

如果你有一台 24 小时开机的电脑，可以：

### 使用 PM2 管理进程（Windows/Linux）

1. 全局安装 PM2
   ```bash
   npm install -g pm2
   ```

2. 启动后端服务
   ```bash
   cd server
   pm2 start server.js --name health-backend
   pm2 startup  # 设置开机自启
   pm2 save     # 保存当前状态
   ```

3. 启动前端服务
   ```bash
   cd ..
   npm run build  # 先构建生产版本
   pm2 start npx --name health-frontend -- serve -s dist -l 3000
   ```

4. 常用命令
   ```bash
   pm2 list               # 查看运行状态
   pm2 logs health-backend # 查看后端日志
   pm2 stop health-backend # 停止服务
   pm2 restart health-backend # 重启服务
   ```

---

## 🔌 端口映射（内网穿透）

### 免费方案：Natapp（无需注册！）

1. 访问 https://natapp.cn/
2. 下载 Windows 版本
3. 运行 natapp.exe
4. 配置本地端口：3000 和 3002
5. 获得公网地址

---

## 📋 检查清单

部署前确认：
- [ ] 代码已上传 GitHub
- [ ] 依赖已安装（npm install）
- [ ] 数据库已初始化
- [ ] 前端 API 地址已配置
- [ ] 端口配置正确（3000/3002）

---

## 🎉 开始部署

最简单的方法：使用 Railway！

**完整步骤（3分钟搞定）**：
1. 代码上传 GitHub
2. 打开 Railway.app，一键连接 GitHub 仓库
3. 点击部署，等待完成
4. 获得永久公网地址！

---

**有问题随时问我！** 😊

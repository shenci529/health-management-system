# 🌐 健康管理系统 - 公网地址配置指南

## 📌 方案说明

我们将使用 **ngrok** 内网穿透工具，它可以将你的本地服务暴露到公网，生成一个永久的访问地址。

### 优点：
- ✅ 不需要知道服务器IP
- ✅ 不需要配置路由器
- ✅ 支持HTTPS（更安全）
- ✅ 可以固定子域名（付费版）或随机地址（免费版）
- ✅ 随时可以访问

---

## 🚀 快速配置步骤

### 第一步：下载ngrok

1. 访问 https://ngrok.com/download
2. 下载 Windows 版本
3. 解压到项目根目录（和 start.bat 同级）

### 第二步：注册账号获取Token

1. 访问 https://ngrok.com/signup
2. 使用邮箱注册账号
3. 登录后进入 Dashboard
4. 复制你的 Authtoken

### 第三步：配置ngrok

双击运行项目根目录下的 `设置公网地址.bat`

或者手动配置：
```
ngrok config add-authtoken <你的Token>
```

### 第四步：启动服务

1. 双击 `一键启动公网.bat`
2. 等待几秒钟
3. 会显示两个地址：
   - `http://localhost:4040` - ngrok管理界面
   - `http://xxx.ngrok.io` 或 `https://xxx.ngrok.io` - **这就是你的公网地址！**

---

## 🎯 使用公网地址

获得公网地址后，访问方式：

### 前端应用
```
http://xxx.ngrok.io      (端口80，默认)
或
https://xxx.ngrok.io     (更安全，推荐)
```

### 后端API
```
http://xxx.ngrok.io:3002
```

### 管理界面
```
http://localhost:4040
```

---

## 💡 常见问题

### Q: 地址每次重启会变吗？
**免费版**：会的。每次启动ngrok都会获得一个新地址。
**付费版**：可以固定子域名，永远是同一个地址。

### Q: 免费版有什么限制？
- 每月带宽流量限制
- 同时只能运行1个隧道
- 连接数限制

对于学习和小规模使用足够了！

### Q: 如何让地址永久不变？
1. 注册ngrok付费账号（约$5/月）
2. 购买固定子域名（如 `health.xxx.ngrok.io`）
3. 配置到 ngrok.yml 中

### Q: 安全吗？
- ngrok使用加密传输
- 可以设置访问密码
- 支持HTTP Basic认证
- 建议仅用于开发和测试

---

## 🔧 手动命令参考

### 启动后端服务
```bash
cd server
npm start
```

### 启动前端服务
```bash
npm run dev
```

### 启动ngrok隧道（后端）
```bash
ngrok http 3002
```

### 启动ngrok隧道（前端）
```bash
ngrok http 3000
```

---

## 📝 配置文件示例

在 `ngrok.yml` 中配置（高级用法）：

```yaml
authtoken: <你的Token>
version: "2"
region: us
tunnels:
  backend:
    addr: 3002
    proto: http
    subdomain: health-api
  frontend:
    addr: 3000
    proto: http
    subdomain: health-web
```

---

## 🎉 推荐做法

### 方案1：固定端口转发（简单）
运行 `一键启动公网.bat`，会自动创建两个隧道：
- 前端： http://localhost:3000 → 公网
- 后端： http://localhost:3002 → 公网

### 方案2：使用代理（推荐）
前端页面中直接配置固定的后端地址。

---

## 📞 技术支持

如果遇到问题：
1. 检查 ngrok 是否正常安装
2. 检查 Token 是否配置正确
3. 检查端口是否被占用
4. 查看 ngrok 日志输出

---

**配置完成后，你就可以在任何地方访问系统了！** 🌍
